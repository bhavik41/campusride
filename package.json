{
  "name": "campusride",
  "version": "1.0.0",
  "description": "CampusRide - Ride sharing platform for college students",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "setup": "npm run install:all && npm run db:setup",
    "db:setup": "npm run --prefix backend db:generate && npm run --prefix backend db:push && npm run --prefix backend db:seed",
    "dev:backend": "npm run --prefix backend dev",
    "dev:frontend": "npm run --prefix frontend dev",
    "build:backend": "npm run --prefix backend build",
    "build:frontend": "npm run --prefix frontend build"
  }
}
